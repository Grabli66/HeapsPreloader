class Main {
    static function main() {
        loader.Packer.init ();
        //Sys.programPath().split("\\").join("/").split("/");
        //hxd.Res.initEmbed ();
        new TestApp ();
    }
}
